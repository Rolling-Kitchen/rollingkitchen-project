<%= render 'shared/banner' %>
<%= render 'foodtrucks/foodtype' %>
<%= render 'foodtrucks/foodtruck-cards' %>


<%= javascript_pack_tag 'isotope_filters'%>

<div class="search-bar">
                        <%= form_tag foodtrucks_path, class: 'search-field', method: :get do %>
                        <%= text_field_tag :query,
                            params[:query], class: "form-control", placeholder: "Find a foodtruck by location, cuisine or name"%>
                        <%= submit_tag "Search", class: "btn btn-primary" %>
                        <% end %>
                    </div>